subprojects {
    apply plugin: 'java'

    repositories {
        mavenCentral()
        flatDir {
            dirs '../trace/build/libs'
        }
    }
    sourceCompatibility = '17'
    targetCompatibility = '17'
}
